# Build and P0 verification report

As of September 7, 2026. Base: `758186b`; the following implementation commit includes this report. This is a
chronological log, not a v1 release sign-off. Early NOT_RUN notes describe their point in time; current overall
status is in [STATUS](../STATUS.md).

## Environment and commands actually run

| Check | Result | Level |
|---|---|---|
| Existing files/git | handoff package only; new `main` created | LIVE_VERIFIED |
| GitHub | public `qwertz92/SourceScribe` created, base pushed | LIVE_VERIFIED |
| Network to Google SDK/Maven/GitHub | HTTPS reachable | LIVE_VERIFIED |
| `java -version` | OpenJDK 17.0.20.1 | LIVE_VERIFIED, WSL |
| Gradle `--version` | 9.6.0, distribution SHA-256 `bbaeb2fef8710818cf0e261201dab964c572f92b942812df0c3620d62a529a01` | LIVE_VERIFIED |
| Windows SDK `adb.exe devices -l` | `emulator-5554 device` | LIVE_VERIFIED |
| ADB `getprop ro.build.version.sdk` | 37 (Android 17) | LIVE_VERIFIED |
| ADB `getprop ro.product.cpu.abilist` | `x86_64,arm64-v8a` | LIVE_VERIFIED |
| ADB `getconf PAGE_SIZE` | 16384 | LIVE_VERIFIED |
| Physical ARM64 device | not connected | BLOCKED |

The Android build runs in WSL; ADB uses the existing Windows access. No ADB port exposed publicly; no emulator
restart and no global security changes. Separate work caches/SDK under `/tmp/sourcescribe-*`; `local.properties`
stays local and points at `/tmp/sourcescribe-tools/sdk`. Installed build tools: 37.0.0; AGP additionally resolved its
default 36.0.0. Platform 37.0 from the existing SDK.

## Build and first deterministic checks

Actually run, each with a 600-second limit:

```bash
timeout 600s env GRADLE_USER_HOME=/tmp/sourcescribe-gradle \
  ANDROID_USER_HOME=/tmp/sourcescribe-android \
  ./gradlew :core:test :app:assembleDebug --console plain \
  '-Dorg.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8 -Duser.home=/tmp/sourcescribe-java-home'
```

Result on the second successful build: `BUILD SUCCESSFUL in 30s`. Built together: AGP 9.4.0 / Kotlin 2.4.20 /
Compose BOM 2026.08.00. 7 JVM tests passed, 0 skipped: SourceAndPlannerTest (4), ExtractorMetadataTest (3). Covered:
canonical sources, host/identity attacks, share deduplication, mode/fallback limits, live-stream rejection, caption
provenance/translations, ambiguous audio tracks.

APK: `app/build/outputs/apk/debug/app-debug.apk` (initially about 136MiB, both ABIs, debug). Its existence alone does
not prove extraction works.

## Build errors fixed and tooling notes

- First build: the Kotlin daemon tried writing into a read-only user directory; fixed with
  `kotlin.compiler.execution.strategy=in-process`, recompiled cleanly afterward.
- Native strip note: audited upstream executables and ZIP containers named `.so` are deliberately packaged
  unmodified; selective `keepDebugSymbols` entries document this integrity decision.
- Remaining environment note: AGP tries to reach `/root/.android/analytics.settings` for its metrics init and
  reports read-only. Build/test pass; no app-code warning and no global write access granted.
- Setting `ANDROID_PREFS_ROOT` and `ANDROID_USER_HOME` together failed already at plugin configuration; the
  documented, actually successful call above uses only `ANDROID_USER_HOME`.

## Still-outstanding P0 evidence

Android execution of Python/JS/EJS/FFmpeg/TLS, native ABI audit, real metadata, caption and audio, process abort,
and a cryptographically verified component update with activation/rollback: NOT_RUN or in progress. No provider
requests executed; live provider access without a test budget is BLOCKED.

## Further P0 integration (no phase sign-off yet)

Examined Maven AARs: `library`, `common`, `ffmpeg` from wrapper 0.18.1. Contain: Python 3.12.11, QuickJS 2025-04-26,
FFmpeg 7.1.1; initially yt-dlp 2025.11.12/EJS 0.3.1. These values come from artifacts, not yet from successful
Android execution. The custom runner uses only the wrapper's initialization, not its updater or shell-abort logic.

Native audit via `readelf -lW/-d` for ARM64 and x86_64: Python and the outer executables are 16KB-aligned. Five
inner FFmpeg WebP ELFs have `p_align=0x1000`: libwebp, libwebpmux, libsharpyuv, libwebpdecoder, libwebpdemux; FFmpeg
loads the first three via libavcodec. Their offsets are also not 16KB-congruent; repackaging the ZIP alone does not
fix this. Source basis for a targeted rebuild: Termux libwebp recipe `b279d510b0ee2d35ba76c8928e736532edb340a9`.

The official yt-dlp zipapp 2026.08.19 contains EJS 0.8.0. SHA-256
`1fa6733c37ea6fb51c99ad8fe785e7b7e5f3246c9b980230329d4fb72ed8d4d6`. `gpgv` confirmed the signed `SHA2-256SUMS` with
fixed fingerprint `AC0CBBE6848D6A873464AF4E57CF65933B5A7581`. The verified zipapp is used as a bundled resource;
Kotlin verifier and Android activation are tested separately. The key is not fetched from the network on every
update.

Integration builds surfaced real bugs: missing JSON extension imports, a JVM signature collision in the exporter,
and an invalid caption-tag regex. These were fixed; the regex has a regression test. The new test suite is
explicitly not PASS until it completes fully. `Process.pid()` was replaced with a custom launcher and verifiable
process data due to missing API availability. Duplicate license files from the PGP dependencies are merged, not
removed, in the APK.

## Resumption after WSL restart, September 7, 18:54 CEST

Run r21: 81 JVM tests, three failures. Run r22
(`bash tools/build-local.sh :core:test :app:assembleDebug :app:assembleDebugAndroidTest :extractor:assembleDebugAndroidTest`):
the JVM part passed 83 tests, 0 failures. The Android build/device test was not yet finished at this point; no
Android sign-off follows from the JVM tests alone.

- ArtifactFiles: 11; CaptionParser: 10; TranscriptExporter: 7.
- EngineVerifier: 8; ExtractorMetadata: 3; SourceAndPlanner: 4.
- ProviderHttp: 9; AssemblyAiAdapter: 19; SyncProvider: 12.
- Fixed repros: `retainRaw=true` without raw data, raw integrity binding, symlink-capable storage paths, bounded
  reads.
- Two wrong test preconditions fixed: a blocking `takeRequest` never let the started coroutine proceed; a supposedly
  descending time sequence was actually ascending. The fixture now waits on an IO thread and genuinely uses
  descending start times, respectively.
- These tests use local fixtures, not real provider requests.

Raw reports are under `core/build/test-results/test/`; local build logs are kept under
`.local-tools/build-reports/` on the project drive after the restart.

## Android evidence after recovery (r26-r32)

All commands now use `timeout 1200 bash tools/build-local.sh ...` and `.local-tools/build-reports/` on the project
drive; the `/tmp` paths above describe only the historical starting run. WSL swap is genuinely active at 16GiB.

- r26: `:app:assembleDebug` PASS; test-APK compilation FAILED (type errors in test content providers and a missing
  fixture receiver). Fixed.
- r27/r29/r30: `:extractor:assembleDebugAndroidTest` PASS.
- r28: 85 JVM tests PASS; r30/r32: 86 JVM tests PASS (added a raw-ZIP round-trip).
- r27 first device run: **INVALID EVIDENCE** for the new code. Install had failed due to a changed temporary debug
  key; the old test package was still running. The pure instrumentation package was removed and freshly installed.
- r27b: 13 registered tests, 8 PASS, 4 FAIL, 1 not run for lacking a source argument. FFmpeg/ffprobe, timeout, abort,
  process cleanup, and DNS/TLS all PASS.
- r29: 6 tests, 1 PASS, 5 FAIL. The direct verifier check hit Android's
  `ZipException: Entry at offset zero has invalid LFH signature 752f2123`. The upstream signature/hash check had
  passed. A controlled ZIP view without an exactly checked 23-byte Python start line fixes the archive check;
  storage and execution still use the unmodified signed original.
- Real ADB counter-check: ffprobe 7.1.1 rejects `-nostdin` with `Option not found`. The flag stays only on the
  FFmpeg call; the native runner closes stdin regardless.
- r30 Android: 16 registered tests, 14 PASS, 1 FAIL, 1 not run for lacking a source argument. Direct check of the
  real bundled package, including a tampered manifest, PASS. All five audio-preparation/symlink tests PASS. The
  remaining failure was handling native Android data aliases in the engine-slot guard; r32 checks this integration
  and the real source.
- App `app.sourcescribe.debug`: fresh install and start PASS; German Compose hierarchy actually read via
  UIAutomator. No startup crash in the targeted AndroidRuntime log query. This does not yet replace a P1
  end-to-end pass.

Device unchanged: `emulator-5554`, API 37, x86_64, 16,384-byte pages. All STT provider live checks remain BLOCKED (no
approved credentials). Local raw evidence: `android-native-r27b.txt`, `android-verifier-audio-r29.txt`,
`android-native-r30.txt`, `android-extraction-r32.txt` in the report directory above.

### r33: QuickJS help output, not a runtime defect

86 JVM tests passed. The previous Android run r32 ended with 10 tests, 6 passed and 4 failed; all four failed before
reaching YouTube, at the shared runtime check. The targeted r33 run,
`NativeRuntimeTest#bundledVersionsExposeNativeTlsAndEjs`, isolated the cause to QuickJS `-h`: the process prints
valid version help but exits with status 1. Reproduced directly on Android (`quickjs-help-r33.txt`). In the actually
loaded official source archive
[QuickJS 2025-04-26](https://bellard.org/quickjs/quickjs-2025-04-26.tar.xz), `qjs.c`'s `help()` function
explicitly calls `exit(1)`. The check now accepts 0 or 1 for this help output given correct usage/version
text; the separate JavaScript evaluation must still end successfully with 0. This is not yet proof of the YouTube
chain; the corrected Android run follows.

### r34: Android runtimes and real metadata demonstrated

On the API-37/x86_64/16KB emulator, the actual NativeRunner reports: Python **3.12.11**, OpenSSL **3.5.2 (5 Aug
2025)**, TLS minimum **TLSv1_2**, QuickJS **2025-04-26**, yt-dlp **2026.08.19**, EJS **0.8.0**, FFmpeg/ffprobe
**7.1.1**. The JavaScript evaluation and the full version/compatibility check pass.

The first public run, `android-extraction-r34.txt`, has 10 tests, 7 passed and 3 failed. The three source tests
explicitly report "This video is unavailable" for the earlier yt-dlp test video `BaW_jenozKc`. No source was swapped
within an app job. For a separate new test, `jNQXAC9IVRw` was explicitly chosen (YouTube oEmbed confirmed "Me at the
zoo", channel "jawed"). `android-public-zoo-r34.txt`: metadata run passed, exact ID confirmed, two caption languages
reported. The two integrated caption/audio cases then still fail in the app's metadata parser with
`INVALID_CAPTION_URL` — a reproducible integration defect under investigation. There is still no complete
caption/audio end-to-end proof.

The Gradle checksum list was generated with `--write-verification-metadata sha256`; it binds the files actually
fetched over the configured HTTPS repositories — a verifiable integrity baseline, not a claim of PGP-signed Maven
artifacts. Wrapper hash, Actions pinning, secret/main source boundaries, and the metadata file pass
`python3 tools/check-repository.py`. The following build runs with normal dependency verification enabled.

## Android extraction and update after resumption (r38)

`TESTED_WITH_FIXTURES`: 89 JVM tests, 0 failures, 0 skipped (`:core:test` r38 XML results). 44 Android tests for DB,
migration, keystore, storage budget, diagnostics, and STT recovery already passed separately in r37. An earlier full
app test run failed on Kotlin references inside separately running test content providers; these Android test
helpers were moved to Java with no Kotlin runtime dependency. Their new full run is still pending. r38 built the main
app but failed at test compile: an agent added `restore` tests after the main-compile snapshot. The following app
build starts after the full handoff; this is not a passed app test.

`LIVE_VERIFIED`: `ExtractionChainTest`, against the explicitly configured public source
`https://www.youtube.com/watch?v=jNQXAC9IVRw`, r38, **2 tests passed** in 46.920s on the API-37/x86_64/16KB emulator.

- Caption: `youtube:jNQXAC9IVRw`, `subtitles:en:json3`, 918 bytes, 6 segments, technically complete; SHA-256
  `1fea290ac341d2b2c79f26a483e653b5904649dcf8c756cf2c7bcd3543f7931e`.
- Audio: same source ID, track `139-drc`, 117,454 bytes; ffprobe duration 19,133ms.
- FFmpeg preparation output: 81,071 bytes, SHA-256
  `b5438f5a04a5ef4217ad02a437c7266f64b3d0a625af3123d634d39eab532257`.
- Command run: `adb -s emulator-5554 shell am instrument -w -e class app.sourcescribe.extractor.ExtractionChainTest
  -e publicSourceUrl https://www.youtube.com/watch?v=jNQXAC9IVRw
  app.sourcescribe.extractor.test/androidx.test.runner.AndroidJUnitRunner`.

Metadata selection now follows yt-dlp's actual format preference; caption fetch runs with fixed arguments in the
Android runtime. Provenance and source ID are re-checked after fetch. The successful live case uses JSON3; an
HLS-only live case is explicitly not yet demonstrated.

`TESTED_WITH_FIXTURES`: the EngineUpdateManager/Verifier/AudioPreparation round r38, no failures, 19 registered
tests, with the separately-gated live-update case skipped. Test context paths were adjusted to the actually checked
private engine root; the production path check stayed strict.

`LIVE_VERIFIED`: `realReleaseStageActivateAndRollbackSurvivesManagerRestart` with `sourcescribeEngineUpdate=true`,
`sourcescribeEngineLiveUpdate=true`, `engineProbeSource=https://www.youtube.com/watch?v=jNQXAC9IVRw`, and
`engineUpdateChannel=NIGHTLY`: 1 test passed, 69.618s (r38b). The package was fetched on Android, signature-checked,
started, activated against the same source, and rolled back to the previous package via a new manager instance.
Exact new component identity is additionally output in the following evidence run. No backend service and no paid
provider API were involved.

## P1 — real operation on the emulator (app APK r38)

`LIVE_VERIFIED`, September 7, 2026, about 21:22-21:25 CEST: the Android share intent opened the unmodified link
`https://www.youtube.com/watch?v=jNQXAC9IVRw`. In Compose, "YouTube only," "Check source," the explicit English
uploader track, and "Confirm and start" were operated. Notification permission was denied. The job still finished
successfully; the UI pointed to history for progress.

Without an export folder: internal success and a separately reported missing export permission. The viewer showed
six real segments with YouTube/channel provenance and language `en`. Afterward, via the real Android folder picker,
`Documents/SourceScribe-Test` was created, access explicitly granted, and Markdown exported. History shows this
export as saved; the original failed export attempt stays traceable. "Share file" opened the Android chooser with
the generated Markdown file. No recipient selected.

Verified by reading the actual internal and external files via ADB:

- Artifact `ffbcec42-3906-4b83-a28b-9fa46e02c6d8`, source `youtube:jNQXAC9IVRw`.
- Canonical JSON 2,874 bytes, SHA-256 `49ebef9f3218128c91de1e0285d413ee2d924ee4449f64d44fac5be4941184ec`.
- External Markdown 3,323 bytes, SHA-256 `7054351f403b8e6622b0d96bff2e46595045f32704a76a63352a41306967dcd2`.
- All six segment texts plus source and artifact ID present in the exported file; automatic content comparison
  PASS. No re-extraction triggered by export. Contents are not stored in the public test report.

Additional UI review fixes (rotation, preview-revision protection, explicit key restore, and complete clipboard
sections) were implemented only afterward and need their own follow-up build/ADB evidence.
`extractor:lintDebug` r40: PASS after a narrow, justified `UsableSpace` exception in the central physical-storage
guard; no lint baseline or severity downgrade.

### Exact update identity (evidence run r40)

After reinstalling the r40 test APK, the same real update/rollback test passed again, 71.054s. Android instrumentation
reported:

- New nightly: `2026.08.30.232658`, EJS `0.8.0`.
- Git head `bbc809a1161d3bfca51fa36f59dda35556ee85a0`.
- SHA-256 `3f1b267b4488f3aed3731a9e84a44011ca5569901868532e10ee11fd07d69707`.
- Rollback `2026.08.19`, SHA-256 `1fa6733c37ea6fb51c99ad8fe785e7b7e5f3246c9b980230329d4fb72ed8d4d6`.

This demonstrates the actual component swap on Android. It does not prove compatibility with any future
Python/EJS/native combination: an unsuitable package is still rejected before activation, or needs an app update.

## r48: real extraction repeated, source binding tightened

September 7, 2026, Gradle 9.7.1/Java 17.0.20.1. Actually run:

```text
tools/build-local.sh :core:test :extractor:assembleDebugAndroidTest :extractor:lintDebug
adb -s emulator-5554 install -r extractor-debug-androidTest.apk
adb -s emulator-5554 shell am instrument -w -e class app.sourcescribe.extractor.ExtractionBoundaryTest,app.sourcescribe.extractor.ExtractionChainTest -e publicSourceUrl https://www.youtube.com/watch?v=jNQXAC9IVRw app.sourcescribe.extractor.test/androidx.test.runner.AndroidJUnitRunner
```

Build succeeded in 10min 5s; 97 JVM tests and strict extractor lint passed. Device unchanged, API 37/x86_64/16KB.
Instrumentation: 6 tests passed in 34.024s, four of them Android fixture counter-checks (`TESTED_WITH_FIXTURES`) and
two real caption/audio checks (`LIVE_VERIFIED`). An encoded foreign video ID, a duplicated query parameter, and a
wrong effective language are all rejected.

Real source `youtube:jNQXAC9IVRw`, caption `subtitles:en:json3`: 918 bytes, 6 segments, technically complete,
SHA-256 `1fea290ac341d2b2c79f26a483e653b5904649dcf8c756cf2c7bcd3543f7931e`. Audio `139-drc`: 117,454 bytes, ffprobe
duration 19,133ms; prepared clip 81,071 bytes, SHA-256
`b5438f5a04a5ef4217ad02a437c7266f64b3d0a625af3123d634d39eab532257`. Local logs: `build-r48-core-extractor.log`,
`install-extractor-r48.log`, and `android-extraction-r48.txt` under `.local-tools/build-reports/`. No real STT API
was called. This does not yet demonstrate a full app regression pass.

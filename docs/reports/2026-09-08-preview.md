# Personal preview 0.1.0-preview.1 — September 8, 2026

Secured app source state: `b631bbe`; prepared CI diagnostics `f9d4f8b` on `main`. The app is classified as a
**personal preview**, not a full v1 release. The chronological bug/fix history stays in the
[integration report](2026-09-07-integration.md); real extraction and the earlier app end-to-end pass are in the
[P0/P1 report](2026-09-07-build-and-p0.md).

The bounded close-out after renewed user approval covers r81: installed the current r80 APKs, re-passed 180 app
tests, checked a history screenshot and release contents, created a durable personal signature. Paused afterward.
The CI device test kept failing; details below. Portable measurements and raw-evidence hashes:
[preview-evidence.json](2026-09-08-preview-evidence.json).

## Gates run

| Check | Actual result and scope |
|---|---|
| JVM/Core | PASS: 103 tests, 12 suites, no failures/skips. Last run r52; core unchanged since, later builds report UP-TO-DATE. XML re-checked. |
| Full build r68 | PASS: 16min 43s, 257 tasks. Debug, AndroidTest, unsigned release, and app/extractor lint debug and release. |
| Production change r69 | PASS: 9min 50s, 216 tasks; debug, AndroidTest, unsigned release, app lint debug/release. |
| Test fixes r70/r71 | PASS: 3min 33s / 4min 23s, 111 tasks each; test APK and app debug lint. Historical checkpoint before the history fix r77. |
| Final history fix r77 | PASS: 12min 3s, 216 tasks (55 executed); debug, AndroidTest, unsigned release, app lint debug/release. |
| Pause build r80 | PASS: 10min 19s, 216 tasks (29 executed); debug, AndroidTest, unsigned release, app lint debug/release. Followed by device run/screenshot/release audit r81 PASS. |
| Source consistency | 145 source/build file hashes identical before/after r68-r71, r74, r77, r79, and r80; no build input changed during a build. |
| Lint | All four current XML reports: 0 issues. Known host warning: AGP cannot write `/root/.android/analytics.settings` in the sandbox context; no global security change made for it. |
| App instrumentation r81 on r80 APK | PASS: **180 tests run**, 124.41s. The runner reports 185 cases: 180 status 0, five status -4 (opt-in). `UiFixtureTest` deliberately excluded to avoid recreating its existing synthetic file. |
| T26 engine pinning r70 | PASS: 81.72s. Two real coordinator runs with synthetic captions during a real signed engine activation; both keep their pinned installation, a later job gets the new engine. No live STT. |
| T33 local import r68 | PASS: import 1.811s; URI grant revoked, external synthetic WAV deleted; force-stop; new process reads the same internal file/Room metadata/hash in 0.229s. |
| T23 private data r68 | PASS: 14 tests in 6.435s. Persisted acquisition/export WorkData contains only `attemptId`. 14 canaries checked against 199 fresh logcat lines from both test UIDs: 0 hits, scoped to this run. |
| Async process/reboot recovery | PASS r59/r61: existing AssemblyAI remote ID reused, one poll GET, no new submission. Provider responses from a local HTTPS fixture. |
| P0 Android execution | LIVE_VERIFIED r48: six checks including real YouTube metadata, captions, audio, FFmpeg preparation, and native process cleanup. |
| Caption app end-to-end | LIVE_VERIFIED r38: Android share to exact source/caption to internal storage/viewer to SAF Markdown with content comparison to share dialog. |

The app run's five opt-in skips are `EngineJobPinningTest` and `ProcessRecoveryTest`'s four stages; their separate
runs are shown above and are not recounted as 185 current passes. The large-viewer test fixture was deliberately
created once beforehand.

## Device, runtime, and update

ADB: `emulator-5554`, Android 17/API 37, x86_64, page size 16,384 bytes, 1280×2856px, density 480. Fingerprint:
`google/sdk_gphone16k_x86_64/emu64xa16k:17/CP31.260618.005/15731206:user/dev-keys`. An ARM64 entry in an ABI list is
not a substitute for physical ARM64 execution.

| Component | Version actually found |
|---|---|
| Java / Gradle / AGP | 17.0.20.1 / 9.7.1 / 9.4.0 |
| Kotlin / Compose BOM / AppCompat | 2.4.20 / 2026.08.00 / 1.8.0 |
| WorkManager | 2.11.2 |
| Android Python / OpenSSL | 3.12.11 / 3.5.2 (5 Aug 2025); TLS minimum TLSv1_2 |
| QuickJS / EJS | 2025-04-26 / 0.8.0 |
| FFmpeg / ffprobe | 7.1.1 / 7.1.1 |
| yt-dlp bundled / activated nightly | 2026.08.19 / 2026.08.30.232658 |

T26 checked real engine bytes before and after the switch; both EJS versions 0.8.0:

- Old: SHA-256 `1fa6733c37ea6fb51c99ad8fe785e7b7e5f3246c9b980230329d4fb72ed8d4d6`.
- New: SHA-256 `3f1b267b4488f3aed3731a9e84a44011ca5569901868532e10ee11fd07d69707`.
- Signature/hash/runtime check and activation probe with an explicit YouTube source are real; the two running
  caption jobs are `TESTED_WITH_FIXTURES`, their wrapper version metadata explicitly `DECLARED_FROM_REAL_OLD_PROBE`.
- Separate real update/rollback run r63: PASS; failed updates additionally tested with controlled
  transport/filesystem/signature fixtures.

## UI and real file output

German/English is a native app language choice, independent of transcription language; system/light/dark stays
independent of it too. The general upload toggle is removed; source, provider, credential reference, and cost
budget are bound internally at the deliberate start — no silent provider switch and no test-cost authorization.

Screenshots actually reviewed show normal German settings, English settings in dark mode, 320×600dp at 200% font,
the viewer, actions, copy selection, and the Android chooser. Selection fields reserve the measured height of their
longest option; no clipped values or jumps on option change. The short navigation labels "Mehr"/"More" fit even on
the small display. The Android semantic test checks the button role and real dialog opening; this is not a full
TalkBack pass.

In the viewer with a long Unicode title at 200% font in landscape, text initially sits under title/search/
provenance. The real scroll counter-check reaches segment 0 fully; "Actions" and "Back" stay reachable — the
initial suspicion of clipped content was disproven, no unnecessary layout change made. 10,000 synthetic segments are
searchable; `SUCHMARKE` reaches segment 9999. Without timing/raw data, the format picker offers MD/TXT/JSON, no
fabricated SRT/VTT/RAW.

The actual generated share file holds 912,857 UTF-8 bytes plus first/last segment; SHA-256
`57535b2781782b4b66ba13e70d4f3ab8c753d5c870f769a6f4b09831f55b484a`. The file name contains source ID, artifact ID,
`model-unknown`, language, and extension. The Android chooser opened and closed with no recipient chosen — nothing
sent.

Publicly viewable, actually reviewed UI examples: [German/system](screenshots/settings-de.png),
[English/dark](screenshots/settings-en-dark.png),
[viewer in landscape at 200% font](screenshots/viewer-landscape-200.png). The transcript file in the last image is
explicitly synthetic.

## APK r80 and current static release check r81

| File relative to repo root | Bytes | SHA-256 |
|---|---:|---|
| `app/build/outputs/apk/debug/app-debug.apk` | 159065550 | `ad94d85da0b1fc3cf3bbc885b3f3fa2857806448d4e20524a3c5971e3a887b32` |
| `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` | 2441468 | `014c339854d4c6a46813fbbed764ad863dd7b09942dd91bcf2f988491c54e649` |
| `app/build/outputs/apk/release/app-release-unsigned.apk` | 147326235 | `ab860009744d54496550c11e77846c3f0f973c37e2ff6e6e26f68b236d10d952` |

The current static release check r81 confirmed with official aapt2: not debuggable, not testOnly; no cleartext HTTP,
no backup. The actually packaged backup/transfer rules contain 18 exclusions. AndroidTest class-descriptor patterns
searched across all four current release DEX files: none found; no fixture providers in the manifest.

528 ELF files re-checked, including the inner Python/FFmpeg archives: correct ABI machine identifiers. The ten known
inner 4KB WebP libraries are replaced on unpacking by the bundled, freshly rebuilt 16KB files for both ABIs; all ten
replacements present/checked, no unresolved ABI/alignment mismatch. This remains a **static check**, not an ARM64
device run.

Official aapt2 maps `dataExtractionRules` via the actual resource table `res/4j.xml`, which holds the 18 exclusions.
Input hash identical before/after the inventory. The inventory helper alone is not a security gate; its incorrect
claim that apksigner was unavailable was discarded — official tools are in the SDK under `build-tools/37.0.0/`.

Personal signing r81: **PASS**. Durable external RSA-3072/PKCS12 key, directory mode 0700, private files 0600.
Signed with `tools/sign-release.sh`; official `apksigner verify --verbose --print-certs` PASS (v3),
`zipalign -c -P 16 4` PASS. All 510 original ZIP entries are byte-identical after signing; only signature metadata
was added. APK: `app/build/outputs/apk/release/SourceScribe-0.1.0-preview.1.apk`, **147376546 bytes**, SHA-256
`410b654fd08bdb2b7f8142dd88f440b7f49ffe123e363dd3607582b9e855c20a`. Certificate SHA-256:
`19d1da9a8fe704082a531faed8a24d966c485aae581a7076dd4b4f66c11d3881`.

The personal release app `app.sourcescribe` was actually installed and started; debug stays separate. VersionName
0.1.0, versionCode 1; first preview tag `v0.1.0-preview.1`. The local key is stored durably; a separate external
backup and upgrading an existing personal release install are NOT_RUN.

## Reproducing and raw evidence

From the project directory, with the SDK/JDK already set up per [BUILD](../BUILD.md):

```bash
timeout 1500 bash tools/build-local.sh :core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintDebug :app:lintRelease :extractor:assembleDebugAndroidTest :extractor:lintDebug :extractor:lintRelease
timeout 45 python3 tools/check-repository.py --self-test
timeout 45 python3 tools/check-repository.py
```

After installing the debug and test APK on the chosen device, with `ADB` and `SERIAL` per [BUILD](../BUILD.md):

```bash
timeout 600 "$ADB" -s "$SERIAL" shell am instrument -w -r -e notClass app.sourcescribe.data.UiFixtureTest app.sourcescribe.debug.test/androidx.test.runner.AndroidJUnitRunner
```

Special opt-in checks and force-stop stages run separately per the integration report; none of these commands use a
live provider argument or a real key. All local raw evidence lives under the ignored `.local-tools/build-reports/`:
`build-r68.log` through `build-r71.log`, `build-r74.log`, `build-r77.log`, `android-app-r77.txt`,
`android-engine-job-pinning-r70.txt`, `android-import-1-r68.txt`, `android-import-2-r68.txt`,
`android-sensitive-data-r68.txt`, `android-canary-logcat-r68.json`, `android-double-share-r62.json` (T15),
`android-process-stage1-r59.txt` / `android-process-stage2-r59.txt` (T11, force-stop),
`android-reboot-stage1-r61.txt` / `android-reboot-stage2-r61.txt` (T11, reboot), `apks-r77.json`,
`release-security-r71.json`, `release-native-complete-r71.json`, and the matching `ui-*.png/.txt`. Final r80 APK
hashes are in `apks-r80.json`; r81 covers the release audit and app device test of these APKs. Further raw evidence:
`android-app-r81.txt`, `release-audit-r81.json`, `release-manifest-r81.txt`, `release-resources-r81.txt`,
`release-backup-r81.txt`, `release-signature-r81.txt`, `release-signed-r81.json`, `release-install-r81.txt`,
`ui-history-r81.png/.txt`. Raw logs/APKs exist locally, not published as git files.

## Remaining release boundaries

- **BLOCKED:** live transcription with AssemblyAI, OpenAI, and Groq. No explicitly approved test accounts, content,
  or budget; zero paid calls.
- **BLOCKED / NOT_RUN:** physical ARM64 device missing. Execution shown covers the named x86_64/API-37/16KB
  emulator.
- **BLOCKED:** full TalkBack traversal/activation. TalkBack was actually bound, but ADB/UIAutomation bypasses the
  needed InputFilter chain, and the emulator console delivered no matching kernel key events. Windows computer-use
  failed before initializing, on the WSL workspace URI. No operation pass can be inferred from the separate semantic
  test.
- **Open license task:** the FFmpeg AAR's corresponding-source and license attribution are incomplete; see the
  [license report](2026-09-07-licenses.md). Personal release signing itself PASSED in r81.

These are concrete evidence and limits, not a claim of being defect-free.

## T01-T34 mapping

The table maps the deterministic required cases to existing tests. It does not imply an additional, deliberately
adversarial live request. Real provider/device checks are listed separately above.

| Cases | Evidence and level |
|---|---|
| T01-T03 | SourceAndPlannerTest, ExtractorMetadataTest, ExtractionBoundaryTest: source identity/input limits; fixture PASS. A real source is covered separately under P0/P1. |
| T04-T05 | AcquisitionModeMatrixTest, AppPipelineTest, ViewRulesTest: four modes, caption-failure/429 retry, and explicit fallback; fixture/Android PASS. |
| T06 | BatchCreationTest, ParallelJobsTest: unchanged snapshots despite a settings change; Android fixture PASS. |
| T07-T09 | Metadata, track, parser, and language fixtures; correct selection/provenance, Unicode/repeats; PASS. |
| T10 | ProviderFailureMatrixTest, AssemblyAiAdapterTest, SyncProviderTest, ProviderHttpTest: all three adapters over controlled HTTPS; PASS. Live provider BLOCKED. |
| T11-T12 | ProcessRecoveryTest and SttRecoveryTest: persisted remote ID, uncertainty, no blind resubmission; Android fixture/process evidence PASS. |
| T13 | SttStepTest, SttHardeningTest, SttMissingRetryTest: limits/chunks/offsets/partial results; PASS. Native audio preparation additionally real. |
| T14 | ParallelJobsTest, DatabaseTest, StorageBudgetTest: two provider fixtures in parallel, third claim locked, isolated abort; PASS. |
| T15 | SourceAndPlannerTest/DB claims plus two real `ACTION_SEND` intents r62: one job/attempt/artifact before and after, zero submissions; PASS. |
| T16 | AppPipelineTest/Planner: separate BOTH artifacts and a targeted retry; Android fixture PASS. |
| T17-T20 | ExportStoreTest, AppPipelineTest, StorageBudgetTest: real OS grant at the test provider, write/file/Room/cleanup limits; Android fixture PASS. No claim of physically filling the device. |
| T21-T22 | Real large synthetic viewer/search/share path; export-format and clipboard limits additionally deterministic; PASS within stated scope. |
| T23 | WorkSpec/credential/diagnostics tests, a bounded logcat canary scan, and static backup rules; PASS within stated scope. |
| T24-T27 | EngineVerifierTest, EngineUpdateManagerTest, EngineJobPinningTest: malicious/error fixtures and real signed activation/rollback separately; PASS. |
| T28-T29 | Pricing/SttStep/ProviderHttp/redirect tests: conservative limits, no foreign secret host; fixture PASS. No live price or org-wide quota claimed. |
| T30 | Language/theme, small display/200% font, rotation, semantics, and real screenshots; PASS within stated scope. Full TalkBack BLOCKED. |
| T31 | Release DEX/manifest/backup/native inventory r71: static PASS. Current r80 content statically re-checked in r81, including official aapt2 resource mapping and personal signing; PASS. |
| T32 | MigrationTest/history/DB reopen: schema migration with no destructive fallback; Android fixture PASS. No historical user install claimed as an upgrade fixture. |
| T33 | Real content-URI import, grant loss, external file deletion, and new process r68: PASS with a synthetic WAV file. |
| T34 | CaptionParser/TranscriptExporter injection cases: content stays data; fixture PASS. |

Not covered by this: physical ENOSPC, manufacturer battery special-casing, deliberate OS kills at every individual
file/Room instruction, or every existing API/ABI/display combination. Errors at the defined I/O boundaries were
deliberately injected; this is not a claim of physical device damage.

## Additional real network/display check r72/r73

r73 passed the additional real network/display check: a caption-only job created offline (flight mode/WLAN/mobile
data genuinely off, no default network), screen off, network restored. The same WorkManager job finished with a new
internal caption artifact, zero STT submissions; Android was Asleep at completion. Network state identical before
and after. The first r72 attempt wrongly expected only Asleep instead of first Dozing and was kept as FAIL — not a
production bug. r73: **PASS** with the same explicit public source `jNQXAC9IVRw` and English caption. Flight mode
on, WLAN and mobile data off; Android reports "Active default network: none." Exactly one caption attempt stays
QUEUED, zero submissions. Afterward: home, display off, network restored. The already-scheduled WorkManager job
finishes without reopening the app, FINISHED/SUCCESS, exactly one additional artifact. Power state Dozing at first,
Asleep by completion. Baseline identical before/after: flight mode 0, WLAN 1, mobile data 1. This is
`LIVE_VERIFIED` for this real caption/Android lifecycle, not an STT proof. Raw evidence:
`android-network-display-r72.json/.txt`, `android-network-display-r73.json/.txt`,
`ui-offline-queued-r73.png/.txt`.

Notification permission was actually denied; the app shows a clear hint about history/background rules. The prior
grant was already false. New temporary permission flags were reset to the freshly measured baseline: grant false,
USER_SET, both USER_SENSITIVE flags, no USER_FIXED. Exact comparison PASS in `ui-notification-restored-r78.json`.
TalkBack stays off.

## Process kill and delayed recovery r75-r78

r75 failed already in test setup: the async source check was not yet done, so the expected track selection was not
yet visible. No new job resulted; the `finally` block restored network state. r76's `am kill` did not actually kill
the process, since Android kept the still-bound app process alive. Both attempts stay FAIL without implying a
production bug.

r78 killed only the previously identified debug app process (by cmdline/UID) via `run-as … kill -9`; PID 24214
genuinely disappeared. The already offline-saved caption job survived. The app was not opened after network
returned; the 100-second test window ended with WAITING_NETWORK and one retry — this original run stays FAIL. A
later, stable Room/WAL read showed the same job (`65d640c7-746c-48fc-91c5-244148f3e3e8`) as FINISHED/SUCCESS, the
same attempt (`ea1a76aa-99c9-4fda-a50b-bfdf67ecdf55`) with one retry, zero submissions. This later raw evidence has
no separate artifact/file-hash proof; its confirmed scope is the final status in the new process. Android had
started a new app process itself (PID 30033); NexusLauncher stayed in the foreground. **LIVE_VERIFIED:** automatic
recovery after a real process kill, though later than 100s. At this later check the display was already back on via
the `finally` block; completion with the display off is covered separately by r73.

Raw evidence: `android-network-display-r75.json/.txt`, `android-network-display-r76.json/.txt`,
`android-network-display-r78.json/.txt`, `android-process-delayed-r78.json`. This caption check proves neither paid
provider calls nor manufacturer-specific battery rules.

## Fixed review and screenshot findings

The job overview showed unused stored provider info for YouTube-only results. That line is now dropped for
`CAPTIONS_ONLY`; in STT-capable modes it is explicitly labeled "STT configuration." An AssemblyAI remote-delete
action now appears only for a finished or aborted job with a real, not-yet-deleted remote handle. The new Room query
returns only job IDs, never remote IDs/secrets. Missing, foreign, duplicate, and deleted handles are covered by the
new real Room test in the passed r77 full run. Final independent Luna review of this fix: no further reproducible
finding.

Further completed fixes and their regressions are chronological in the integration report: SAF persistence failures
with no new STT request, file-based provenance when sharing, safe resumption of uncertain provider states, bounded
retry/budget decisions, and engine pinning during a real signed update. Early test failures, wrong review
suspicions, and remaining external limits are explicitly distinguished there from production bugs.

The final screenshot fix removes a duplicated export-error message. On SUCCESS and SUCCESS_WITH_WARNINGS, "Retry
missing section" is now hidden; "New run" and "different provider" stay available. The review found the warning
case in the first condition; root cause was confirmed at the planner and at latest-attempt aggregation. The fix was
independently statically re-checked. r81: current r80 app installed, 180 tests passed, and the
[after screenshot](screenshots/history-r81.png) actually reviewed — one export note, no missing-retry on success, no
unused provider label. SUCCESS_WITH_WARNINGS additionally statically checked at the planner/history branch; no
separate warning screenshot claimed as evidence.

## CI and historical pause attempts

[Run 34252821287](https://github.com/qwertz92/SourceScribe/actions/runs/34252821287) at `558527f`: build/JVM/APK/
lint PASS; the emulator path was fixed, real boot 47.555s, ADB device detected, 16KB check passed. Then
`:app:connectedDebugAndroidTest` FAILED after 2min 34s. The console log only gave "There were failing tests," no
specific cause; `ci-558-failed.txt` stays as raw evidence. This is an open CI failure, not an emulator-start
timeout and not yet an identified production defect.

Before the pause, targeted failure extraction from JUnit XML/UTP logs was implemented; bash/Python syntax and
extracting a synthetic error message: PASS. An actual CI run of this diagnostic: NOT_RUN. No test was removed or its
result reinterpreted. The runner emulator raised its own `-memory 2048` to 4096MiB; future config names 4096
explicitly. Its recommended 16,384MiB host RAM sits just above the runner's reported 15,989MiB; the boot still
succeeds. This does not change any local WSL memory limit.

The interim inventory audit `release-audit-r77.json` had already found r79 bytes instead of the expected r77 hash,
explicitly not a consistent r77 pass. The intermediate APK read during that audit was stable and showed no
additional ABI/replacement-asset deviation; this interim state was closed by the consistent r81 audit of the final
r80 bytes. Official aapt2/apksigner exist in the local SDK; the inventory helper's differing availability assumption
is not adopted.

The earlier pause safeguard only let r80 finish. Only the renewed explicit user approval for small close-out work
and the personal test build enabled r81. No further CI run during this bounded close-out; `[skip ci]` on the
documentation push is not a CI pass. The 145 source/build hashes were identical before/after r80. All historical
subagents are done; a final close review of the handoff is separate. Current entry point: [STATUS](../STATUS.md) (then HANDOFF.md).

## Personal release smoke test r81

**LIVE_VERIFIED:** the signed app `app.sourcescribe` was freshly installed and run with CAPTIONS_ONLY, no providers
configured. Exact public source `jNQXAC9IVRw`, English channel-provided caption, history shows Completed/
Successful. Android's first-time notification dialog was declined. After an explicit force-stop and app restart,
the saved result was available in the viewer; expected caption text and the exact URL/ID in expanded provenance
details were checked, and the real viewer screenshot reviewed.

The first smoke-test attempt expected the URL in the still-collapsed detail section and aborted; the same source
check passed after actually expanding it — not a production bug. The real transcript's raw text was not copied into
the check log; saved check values/hash are in `release-smoke-r81.json`, the image only locally at
`release-viewer-r81.png`. SAF export of the **signed** app is NOT_RUN in this close-out; the real debug-app export
is demonstrated separately. No real STT call.

Afterward, app language set to German. The existing adaptive vector icon (document and audio waveform) was checked
and rendered as a preview; no new AI-generated art and no app build change. Review of the portable documentation:
five earlier handoff/SDK/ADB gaps closed, and an additional required `ANDROID_HOME` note added for device commands
when only `local.properties` is set. Repository self-test, repository checks, local Markdown links, and diff
whitespace all passed; no test/lint weakened to force a green result.

Final close independent review: the SDK/ADB guide fix is confirmed, no further findings in this bounded scope. App
language German, TalkBack off, font scale 1. The personal app, debug app, and both test packages were stopped in a
controlled way; `pidof` afterward empty for each. No new CI run started. Raw evidence `preview-pause-r81.json`. The
shared emulator and ADB server remain running.
